apiVersion: 1
datasources:
  - name: Prometheus (Main)
    type: prometheus
    url: http://prometheus:9090
    isDefault: true

  - name: Alertmanager
    type: alertmanager
    url: http://alertmanager:9093
    jsonData:
      implementation: prometheus
