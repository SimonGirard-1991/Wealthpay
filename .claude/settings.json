{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(java|md|yml|yaml|json|xml|properties|sh)$' && mvn -q spotless:apply; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Applying Spotless formatting..."
          }
        ]
      }
    ]
  }
}
